<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F5F5F5"
    tools:context=".CarroActivity"> <!-- Altere para o nome da sua Activity/Fragment -->

    <!-- Pode adicionar um título aqui se quiser -->
    <TextView
        android:id="@+id/tv_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Meus Veículos"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="@color/verdeescuro"
        android:fontFamily="@font/poppins_medium"
        android:padding="16dp"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_car_list"
        android:layout_width="match_parent"
        android:layout_height="195dp"
        android:layout_below="@id/tv_title"
        android:clipToPadding="false"
        android:paddingBottom="80dp"
        tools:listitem="@layout/item_card_carro" /> <!-- Referencia o item que criamos -->

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_add_car"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_margin="16dp"
        android:src="@android:drawable/ic_input_add"
        app:backgroundTint="@color/verdeescuro"
        app:tint="@android:color/white"
        android:contentDescription="Adicionar novo veículo"/>

</RelativeLayout>
